export interface ProjectFilterValue { keyword: string; fromDate: string; toDate: string }
export default function ProjectFilter({ value, onChange, onSearch, onReset, error, disabled }: { value: ProjectFilterValue; onChange: (value: ProjectFilterValue) => void; onSearch: () => void; onReset: () => void; error?: string; disabled: boolean }) {
  return <form onSubmit={(event) => { event.preventDefault(); onSearch(); }} className="flex flex-wrap items-center gap-2 p-4 sm:px-5 sm:py-5"><input type="search" value={value.keyword} onChange={(event) => onChange({ ...value, keyword: event.target.value })} placeholder="프로젝트명, 클라이언트 회사명 검색..." aria-label="프로젝트 검색어" className="h-10 min-w-[240px] flex-1 rounded-lg border border-[#e2e8f0] px-4 text-[13px]"/><input type="date" value={value.fromDate} max={value.toDate || undefined} onChange={(event) => onChange({ ...value, fromDate: event.target.value })} aria-label="등록일 시작" className="h-10 rounded-lg border border-[#e2e8f0] px-3 text-[13px]"/><input type="date" value={value.toDate} min={value.fromDate || undefined} onChange={(event) => onChange({ ...value, toDate: event.target.value })} aria-label="등록일 종료" className="h-10 rounded-lg border border-[#e2e8f0] px-3 text-[13px]"/><button disabled={disabled} className="h-10 rounded-lg bg-[#102947] px-5 text-[13px] font-bold text-white disabled:opacity-50">검색</button><button type="button" disabled={disabled} onClick={onReset} className="h-10 rounded-lg border border-[#e2e8f0] px-4 text-[13px] font-semibold text-[#64748b]">초기화</button>{error ? <p role="alert" className="w-full text-[13px] text-red-600">{error}</p> : null}</form>;
}
