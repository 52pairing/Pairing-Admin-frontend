import Link from "next/link"; import type { InquiryListItem, InquiryMemberType, InquiryStatus } from "../types";
const memberLabel: Record<InquiryMemberType, string> = { CLIENT: "클라이언트", FREELANCER: "프리랜서", ADMIN: "관리자" }; const statusLabel: Record<InquiryStatus, string> = { PENDING: "대기 중", ANSWERED: "답변 완료" };
export default function InquiryRow({ inquiry }: { inquiry: InquiryListItem }) { return <div className="grid min-h-[58px] grid-cols-[145px_90px_2.4fr_1.1fr_100px_105px_105px_70px] items-center border-b px-4 text-[12px]"><span>{inquiry.inquiryNo}</span><span>{memberLabel[inquiry.writerRole]}</span><span className="truncate pr-5 font-semibold">{inquiry.title}</span><span className="truncate">{inquiry.writerName}</span><span>{inquiry.createdAt?.slice(0,10) ?? "-"}</span><span>{statusLabel[inquiry.status]}</span><span>{inquiry.answeredAt?.slice(0,10) ?? "—"}</span><Link href={`/inquiries/${inquiry.inquiryId}`} className="font-bold text-[#2563eb]">상세보기</Link></div>; }
