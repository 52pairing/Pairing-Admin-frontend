import Link from "next/link"; import type { NegotiationListItem } from "../types"; import NegotiationStatusBadge from "./NegotiationStatusBadge";
const date = (v: string | null) => v?.slice(0,10) ?? "-";
export default function AgentRow({ session }: { session: NegotiationListItem }) { return <div className="grid min-h-[64px] grid-cols-[100px_2fr_1fr_1fr_110px_85px_110px_110px_65px] items-center border-b px-4 text-[13px]"><span className="font-bold">{session.negotiationId}</span><span className="truncate pr-5">{session.projectTitle}</span><span className="truncate">{session.clientName}</span><span className="truncate">{session.freelancerName}</span><NegotiationStatusBadge status={session.status} label={session.statusLabel}/><span>{session.totalRound}회</span><span>{date(session.startedAt)}</span><span>{date(session.endedAt)}</span><Link href={`/ai-agent/${session.negotiationId}`} className="font-bold text-blue-600">상세</Link></div>; }
